environment = "production"

[database]
url = "postgres://localid:CHANGE_ME@localhost:5432/localid"
max_connections = 10

[server]
host = "127.0.0.1"
port = 9130
issuer = "https://id.home.arpa"
signing_key_path = "/var/lib/localid/keys/signing-key.pem"
