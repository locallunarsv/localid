environment = "development"

[database]
url = "postgres://postgres:postgres@localhost:5432/localid"
max_connections = 10

[server]
host = "127.0.0.1"
port = 8080
issuer = "http://127.0.0.1:8080"
signing_key_path = "/tmp/localid-signing-key.pem"
