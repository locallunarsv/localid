[package]
name = "localid-token"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
uuid = { version = "1", features = ["v7"] }
serde = { version = "1", features = ["derive"] }
base64 = "0.22"
serde_json = "1"

localid-session = { path = "../localid-session" }

[dev-dependencies]
