[package]
name = "localid-session"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.45", default-features = false, features = ["std"] }
localid-identity = { path = "../localid-identity" }
uuid = { version = "1.24.0", features = ["v7"] }
