[package]
name = "localid-password-argon2"
version = "0.1.0"
edition = "2024"

[dependencies]
argon2 = { version = "0.5", features = ["std"] }
localid-password = { path = "../localid-password" }
rand_core = { version = "0.6", features = ["getrandom"] }

[dev-dependencies]
localid-credential = { path = "../localid-credential" }
