[package]
name = "localid-token-random"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
rand = "0.8"
sha2 = "0.10"
hex = "0.4"

localid-token = { path = "../localid-token" }
localid-session = { path = "../localid-session" }
